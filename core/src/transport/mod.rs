//! 反向隧道的传输层 (重构 P2)
//!
//! 传输 = 「让服务器监听端口, 并把到来的连接交给本机」的机制, 两种实现 (设计 §3.6):
//! - **标准模式**: sshd 原生 `tcpip_forward` 转发通道 (russh 直连)
//! - **兼容模式**: 会话通道 + 服务器端 python3 桥接助手, 帧协议复用单通道
//!   (云主机安全组件注入 sshd 转发通道时的回退路径)
//!
//! - `frame`: 兼容模式的帧协议编解码 (标记帧/CRC32/分块/注入同步),
//!   纯逻辑、独立单测 (P2a)。
//! - 传输 trait 与两个实现、污染探测策略 (P2b): 引擎调用方将不感知模式差异。

pub mod frame;
